interface:
  display_name: "TimeLedge"
  short_description: "Repo-specific patterns and workflows for TimeLedge"
  default_prompt: "Use the TimeLedge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true